[project]
name = "historyrewind-infra"
version = "0.1.0"
description = "AWS CDK app defining HistoryRewind's infrastructure"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aws-cdk-lib>=2.260.0,<3.0.0",
    "constructs>=10.5.0,<11.0.0",
]

[tool.uv]
package = false
