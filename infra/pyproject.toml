[project]
name = "history-zoomout-infra"
version = "0.1.0"
description = "AWS CDK app defining History Zoomout's infrastructure"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aws-cdk-lib>=2.260.0,<3.0.0",
    "aws-cdk.aws-lambda-python-alpha>=2.263.0a0,<2.264.0",
    "constructs>=10.5.0,<11.0.0",
]

[tool.uv]
package = false
